{
  "name": "radix",
  "description": "Radix theme",
  "private": true,
  "scripts": {
    "setup": "npm install",
    "postinstall": "find node_modules/ -name '*.info' -type f -delete"
  },
  "dependencies": {
    "gulp": "~3.9.0"
  },
  "devDependencies": {
    "gulp-scss-lint": "~0.3.9",
    "jshint": "~2.9.1",
    "gulp-jshint": "~2.0.0"
  }
}
